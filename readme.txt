Requerimientos generales:
Al archivo .html adjunto agergar los scripts necesarios para cumplir con los requerimientos indicados.

Requerimientos específicos:
1.- Agregar al document el evento DOMContentLoaded y que este llame a una funcion llamada "addSpinner"

2.- Programar una funcion llamada addSpinner y otra funcion llamada quitSpinner y que esta tenga una variable que haga objetivo al contenedor con id "spin". Utilice el "getElement" necesario.

3.- Dentro de la funcion addSpinner programar que le agregue al objeto "spin" el estilo display con el valor flex.
Dentro de la funcion quitSpinner programar que le agregue al objeto "spin" el estilo display con el valor none.

4.- Programar que el objeto window, al cumplir con el evento onload llame a la funcion quitSpinner.

5.- Programar que al usuario escribir su nombre en el formulario de la pagina se escriba: "Bienvenido: nombreUsuario" donde nombreUsuario es el dato ingresado en el formulario. Valide que el input no este vacio y que no se muestre nada si el usuario no ha ingresado algun dato en el input.